apiVersion: environment.cloudbees.com/v1alpha1
kind: GitRepository
metadata:
  name: emartinez-fm-microblog-frontend                             
  namespace: previews
spec:
  url: https://github.com/emartinez-fm/microblog-frontend.git
  apiTokenSecretRef:
    name: beedemo-dev-github-token
  autoCreateContexts:                           
  - default 
  autoUpdateContexts:                           
  - default
